// Placeholder entry — real activation lands with the webview host (plan Task 5).
import type { ManifoldContext } from 'manifold'
export function activate(_context: ManifoldContext): void {}
export function deactivate(): void {}
