import type React from 'react'

export const backgroundAgentPanelStyles: Record<string, React.CSSProperties> = {
  wrapper: {
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
    background: 'var(--bg-primary)',
    color: 'var(--text-primary)',
  },
  toolbar: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
    padding: '12px 14px',
    borderBottom: '1px solid var(--border)',
    background: 'var(--bg-secondary)',
  },
  toolbarMeta: {
    display: 'flex',
    flexDirection: 'column',
    gap: 4,
  },
  title: {
    fontSize: 13,
    fontWeight: 600,
  },
  subtitle: {
    fontSize: 11,
    color: 'var(--text-muted)',
  },
  refreshButton: {
    padding: '6px 10px',
    borderRadius: 6,
    border: '1px solid var(--border)',
    background: 'var(--bg-elevated)',
    color: 'var(--text-primary)',
    cursor: 'pointer',
    fontSize: 12,
  },
  content: {
    flex: 1,
    overflowY: 'auto',
    padding: 14,
    display: 'flex',
    flexDirection: 'column',
    gap: 12,
  },
  empty: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100%',
    color: 'var(--text-muted)',
    fontSize: 12,
    textAlign: 'center',
    padding: 24,
  },
  infoCard: {
    padding: 12,
    borderRadius: 8,
    border: '1px solid var(--border)',
    background: 'var(--bg-elevated)',
    display: 'flex',
    flexDirection: 'column',
    gap: 8,
  },
  infoLabel: {
    fontSize: 11,
    color: 'var(--text-muted)',
    textTransform: 'uppercase',
    letterSpacing: '0.04em',
  },
  infoText: {
    fontSize: 12,
    lineHeight: 1.5,
  },
  error: {
    color: 'var(--status-error)',
    fontSize: 12,
  },
  card: {
    padding: 14,
    borderRadius: 8,
    border: '1px solid var(--border)',
    background: 'var(--bg-elevated)',
    display: 'flex',
    flexDirection: 'column',
    gap: 10,
  },
  cardHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 12,
  },
  cardTitle: {
    fontSize: 13,
    fontWeight: 600,
  },
  badge: {
    fontSize: 10,
    textTransform: 'uppercase',
    letterSpacing: '0.05em',
    color: 'var(--accent)',
    background: 'var(--accent-subtle)',
    padding: '3px 6px',
    borderRadius: 999,
  },
  bodyText: {
    fontSize: 12,
    lineHeight: 1.5,
    color: 'var(--text-secondary)',
  },
  statRow: {
    display: 'flex',
    gap: 8,
    flexWrap: 'wrap',
  },
  stat: {
    fontSize: 11,
    color: 'var(--text-muted)',
  },
  sourceList: {
    display: 'flex',
    flexDirection: 'column',
    gap: 6,
  },
  sourceLink: {
    fontSize: 12,
    color: 'var(--accent)',
    textDecoration: 'none',
  },
  feedbackRow: {
    display: 'flex',
    gap: 8,
    flexWrap: 'wrap',
  },
  feedbackButton: {
    padding: '5px 8px',
    borderRadius: 6,
    border: '1px solid var(--border)',
    background: 'var(--bg-secondary)',
    color: 'var(--text-secondary)',
    cursor: 'pointer',
    fontSize: 11,
  },
}
